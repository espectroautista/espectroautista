dnl CSS like comments
changecom(`/*', `*/')

dnl CSS Standard colors
include(COLORS.m4)

dnl Theme constants and macros
include(theme.m4)

dnl Comments...
define(`CSSPATH', `')

dnl vim:sw=8:ts=8:ai:fileencoding=utf8:syntax=m4
